[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# Netlify UI → Environment variables → Redeploy:
#   VITE_API_BASE = https://YOUR-USER-traffic-analysis-api.hf.space
# With or without /api is fine; frontend normalizes it.
