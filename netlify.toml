[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# Netlify UI → Environment variables (then Redeploy):
#   VITE_API_BASE = https://traffic-analysis-pgcc.onrender.com
# With or without /api is fine; frontend normalizes it.
